import { Fade } from "@mui/material";


const DashboardPage = () => {


    return (
        <Fade in={true} timeout={1000}>
            <h1>Dashboard</h1>
        </Fade>

    );
};

export default DashboardPage;
